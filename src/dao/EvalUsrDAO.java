/*
 * Marco Cipriani (c) 2019.
 */

package dao;

public class EvalUsrDAO {

    public void findByUsr(String nickname) { // about you
        // select * (tranne status) from evalusr where nickname = nickname && status = 1
    }

    // made by you
    public void findByAuthor(String nickname) {
        // identica però from evalusr(evalusr) + evalpat(evalusr)
    }
}
